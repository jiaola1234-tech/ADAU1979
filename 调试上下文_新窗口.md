# ADAU1979 采集链路调试上下文

## 目标

当前目标是打通 AX7020 + 两片 ADAU1979 的 I²C 初始化和音频采集链路。暂时不使用 VOFA，先通过 PS UART 打印初始化过程、寄存器状态和错误信息，解决 ADAU 初始化报错。

## 当前串口输出

```text
HelloWorld
ADAU1979 capture DMA test
UART: 921600 8N1 (PS UART1)
IP base=0x44A00000, CTRL=0x00000000, STATUS=0x00001E08
ADAU1979 init error, status=0x00001E08
```

`STATUS=0x00001E08` 按当前 RTL 定义解码为：

- `init_step = 0x1E = 30`
- `init_error = 1`
- `init_done = 0`
- `init_busy = 0`

初始化脚本共有 30 次写操作（步骤 0～29），之后读取两片 ADAU1979 的 `PLL_CONTROL` 寄存器 `0x01`，检查 bit7 `PLL_LOCK`。因此目前的状态只能说明初始化失败，尚不能区分：

1. 30 次写入期间出现 I²C NACK；
2. 读取 PLL 状态时出现 I²C NACK；
3. 读取成功，但 PLL 一直没有锁定。

## 已确认的时钟关系

ADAU 板上有 `OSC1 = 24.576 MHz` 外部音频晶振，经过 33 Ω 电阻连接到 ADAU 的 `MCLKIN`。这个时钟供 ADAU 音频 PLL 使用。

Vivado BD 中连接到 ADAU IP 的 `processing_system7_0/FCLK_CLK0` 是 Zynq PS 产生的 FPGA 内部逻辑时钟，当前为 100 MHz。PS 的输入时钟为 33.333 MHz，PS 内部倍频/分频后产生 100 MHz FCLK。

因此：

- IP 参数 `CLK_HZ = 100000000` 是 AXI/I²C 状态机的 FPGA 逻辑时钟；
- 它不是 ADAU 板上的 24.576 MHz MCLK；
- 当前 100 MHz 配置是正确的，不能把这个参数改成 24.576 MHz；
- `I2C_HZ = 100000` 时，`HALF_DIV = 100M / (100k × 2) = 500`，I²C 约为 100 kHz。

## ADAU 地址和关键寄存器

根据 ADAU1979 数据手册，7 位 I²C 地址低 5 位固定为 `10001`，地址选项为：

```text
0x11, 0x31, 0x51, 0x71
```

当前工程参数为：

```text
U1_I2C_ADDR = 7'h11
U2_I2C_ADDR = 7'h31
```

关键寄存器：

- `PLL_CONTROL = 0x01`
- bit7：`PLL_LOCK`
- bit4 `CLK_S=0`：使用 MCLK
- `0x43`：MCLK 模式、MCS=3，对应 24.576 MHz MCLK、48 kHz 音频（512×Fs）

SDA/SCL 必须有有效上拉，ADAU 板与 AX7020 必须共地。

## 原理图和连接

ADAU 板接口 H4：


| H4 引脚 | 信号 |
|---:|---|
| 1 | SCL |
| 2 | SDA |
| 3 | BCLK |
| 4 | LCLK/LRCLK |
| 5 | SDATAOUT2 |
| 6 | SDATAOUT1 |
| 7 | SDATAOUT4 / U2 SDATAOUT2 |
| 8 | SDATAOUT3 / U2 SDATAOUT1 |
| 9 | PD/RST |
| 10 | GND |

AX7020 J10 当前 XDC 映射：


| 信号 | J10 | FPGA 管脚 |
|---|---:|---|
| `adau_scl` | 3 | W19 |
| `adau_sda` | 4 | W18 |
| `adau_bclk` | 5 | R14 |
| `adau_lrclk` | 6 | P14 |
| `adau_sdata[1]` | 7 | Y17 |
| `adau_sdata[0]` | 8 | Y16 |
| `adau_sdata[3]` | 9 | W15 |
| `adau_sdata[2]` | 10 | V15 |
| `adau_pd_rst` | 11 | Y14 |

连接时至少确认：

- H4-1 → AX7020 J10-3（SCL）
- H4-2 → AX7020 J10-4（SDA）
- H4-9 → AX7020 J10-11（PD/RST）
- H4-10 → AX7020 GND
- SDA、SCL 为 3.3 V 电平并有上拉
- ADAU 板电源、AX7020 电源和信号地共地
- PD/RST 上电时序正确，不能一直处于复位/掉电状态

## 关键工程文件

- RTL 初始化状态机：`ip_repo/adau1979_capture_1.0/hdl/adau1979_i2c_init.v`
- I²C 写主机：`ip_repo/adau1979_capture_1.0/hdl/i2c_master_write8.v`
- I²C 读主机：`ip_repo/adau1979_capture_1.0/hdl/i2c_master_read8.v`
- AXI 寄存器接口：`ip_repo/adau1979_capture_1.0/hdl/adau1979_capture_v1_0_S00_AXI.v`
- 驱动头文件：`ip_repo/adau1979_capture_1.0/drivers/adau1979_capture_v1_0/src/adau1979_capture.h`
- 当前 Vitis 源文件：`vitis_workspace/1979_j10_/src/vitis_adau1979_vofa.c`
- 原始诊断程序：`examples/vitis_adau1979_dma_test.c`
- Vivado BD 脚本：`vivado/scripts/02_replace_ad7606_with_adau1979_bd.tcl`
- BD 文件：`vivado/adau1979_lwip/adau1979_lwip.srcs/sources_1/bd/design_1/design_1.bd`

## RTL 当前行为和诊断风险

`adau1979_i2c_init.v` 当前有 `ack_error_count`，它只统计写操作 NACK。PLL 读操作发生 NACK 时会置 `init_error`，但没有单独计数。PLL 读到 bit7=0 超时也会置 `init_error`。

AXI 偏移 `0x1C` 的 `ERROR_COUNT` 当前输出：

```text
i2s_sync_error_count + i2c_ack_error_count
```

所以当前串口程序必须继续打印：

- 原始 `STATUS`
- `init_step`
- `ERROR_COUNT`（AXI 偏移 `0x1C`）
- I2S frame count
- FIFO level
- FIFO overflow

建议的判断方法：

- `ERROR_COUNT > 0`：优先怀疑 I²C 写 NACK、SDA/SCL 连接、电平、上拉、共地、PD/RST 或地址；
- `ERROR_COUNT == 0` 但 `STATUS` 仍为 error：更可能是 PLL 未锁定、MCLK 未到 ADAU、PD/RST 时序或 PLL 配置问题。

如果这些信息仍不能定位，需要扩展 RTL，暴露以下诊断量：

- 写 NACK 计数；
- PLL 读 NACK 计数；
- PLL 超时标志；
- 最后一次 PLL 读值；
- 当前检查的是 U1 还是 U2。

## 当前正确的处理方向

1. 确认 `vitis_workspace/1979_j10_/src/vitis_adau1979_vofa.c` 已恢复为 `examples/vitis_adau1979_dma_test.c` 的 ADAU 诊断程序，而不是 UART-only 程序。
2. 在初始化失败打印中加入 `ERROR_COUNT`、I2S frame count、FIFO level、FIFO overflow，并解码 `STATUS`。
3. 重新编译 Vitis 应用并下载 ELF；如果 RTL 没改，bitstream 暂时不用重新生成。
4. 根据新的串口输出判断是 I²C NACK 还是 PLL 未锁定。
5. 若修改 RTL 以暴露更细的错误信息，必须重新 package IP、refresh IP、生成 bitstream/XSA、更新 Vitis platform，再编译下载 ELF。

## 不要误判的事项

- `FCLK_CLK0 = 100 MHz` 不是错误，也不代表没有使用外部 24.576 MHz 晶振。
- 只打印 `HelloWorld` 不能证明 ADAU 链路正常；必须看到 I²C 初始化和 PLL 状态。
- 当前报错不应直接通过把地址改成别的值或盲改时钟来处理，先读取 `ERROR_COUNT` 和总线电平/连接状态。
- 用户暂时不使用 VOFA，先通过串口完成 ADAU 初始化诊断。

## 新窗口建议首先执行的动作

请先检查并确认当前源文件内容，然后直接修改打印代码，重新编译下载。新串口输出最好包含类似：

```text
STATUS=0x........ step=.. busy=. done=. error=.
ERROR_COUNT=........
I2S_FRAME_COUNT=........ FIFO_LEVEL=........ FIFO_OVERFLOW=.
```

拿到这组输出后，再决定是检查 I²C 硬件/地址/复位，还是修改 PLL 初始化和 MCLK 相关配置。
